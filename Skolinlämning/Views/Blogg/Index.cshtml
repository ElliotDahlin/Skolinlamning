@model IEnumerable<Skolinlämning.Models.BloggPost>
@{
    ViewData["Title"] = "Index";
}

<h2 class="text-center mt-3">Blog Posts</h2>

<div class="row justify-content-center text-dark">
    @foreach (var item in Model)
    {
        <div class="col-md-7">
            <div class="card mb-5">
                <div class="card-body ">
                    <p class="card-author">Author: @item.Author</p>
                    <h5 class="card-title">@item.Title</h5>
                    <p class="card-text">@item.Content</p>
                </div>
                <div class="card-footer">
                    <form method="post">
                        <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-primary">Edit</a>
                        @if (User.IsInRole("Admin"))
                        {
                            <button asp-action="DeleteConfirmed" asp-route-id="@item.ID" class="btn btn-danger" type="submit" onclick="return confirm('Are you sure you want to delete this item?');">Delete</button>
                        }
                    </form>
                </div>
            </div>
        </div>
    }
</div>
